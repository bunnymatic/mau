<div class="logo-spacer"></div>
<div id="favorites_page">
  <h4><%= current_user.get_name true %>'s Favorites</h4>
  <div class="favorites">
    <ul>
      <% current_user.favorites.each do |f| %>
      <li><%= draw_favorite(f.to_obj, @style); %></li>
      <% end -%>
    </ul>
  </div>
</div>
