.admin-table
  %h1 New role

  = form_for(@role) do |f|
    = render '/common/form_errors', :form => f

    = f.label :role
    = f.text_field :role
    = f.submit 'Create', :class => "formbutton"

.links
  = link_to '[all roles]', roles_path
