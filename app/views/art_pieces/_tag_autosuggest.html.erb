<script type="text/javascript">
  Event.observe(window, 'load', function() {
     $('tags').observe('focus', function() {
     var options={ 
       script:'/tags?format=json&suggest=1&', // form elements tagged on the end
       varname:'input', 
       valueSep: ',',
       json:true, 
       minchars:2,
       shownoresults:false,
       maxresults:10,
       callback: null
     }; 
     var json=new AutoComplete('tags', options); 
     return true;
     });
  });
</script>
<script type="text/javascript" src="/javascripts/autocomplete.js"></script>
