= render :partial => '/flash_notice_error'
.pure-g
  .pure-u-1-1.password-reset
    %h3 Request for Activation Email
    .instructions Type in your MAU account email below. We'll send you a new activation link.
    = semantic_form_for :user do |f|
      = render "common/form_errors", :form => f
      = f.inputs do
        = f.input :email, required: true, placeholder: 'enter your email'
      = f.actions do
        = f.submit 'Submit', :class => 'pure-button pure-button-primary button-large'
    .input-help
      = link_to 'Did you forget your password?', forgot_users_path
