!!! 5
%html
  %head
    %meta{ 'http-equiv' => "Content-type", :content => "text/html; charset=utf-8" }
    %meta{ :name => 'description', :content => (@page_description.nil? || @page_description.strip.empty?) ? "Mission Artists United is a website dedicated to the unification of artists in the Mission District of San Francisco.  We promote the artists and the community. Art is the Mission!" : @page_description }
@page_description}
    %meta{ :name => 'keywords', :content => @page_keywords}
    = render :partial => '/extra_html_head' 
    = render :partial => '/title' 
    = render :partial => '/stylesheets' 
    = render :partial => '/ganalytics' 
    = render :partial => '/javascripts' 
  %body{ :class => "one_column %s " % @browser_as_class }
    - if @_ie7 
      = render :partial => '/ie7_warning' 
    = render :partial => '/noscript_msg' 
    #bg
    = render :partial => '/login_header_bar' 
    #container
      = render :partial => '/header_bar' 
      .hstripe
      = render :partial => '/content', :locals => { :cols => 1 } 
      .hstripe
      = render :partial => '/footer' 
  %script Feedback.init();
