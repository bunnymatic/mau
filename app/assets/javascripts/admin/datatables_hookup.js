// Generated by CoffeeScript 1.12.7
(function() {
  jQuery(function() {
    var config, isNotSearchableTable, isSearchableTable, tables;
    if (!jQuery.fn.dataTable) {
      return;
    }
    tables = "body.admin .js-data-tables";
    config = {
      artists_index: {
        order: [[3, "desc"]]
      }
    };
    isSearchableTable = function(_idx, el) {
      return !$(el).hasClass('js-data-tables-no-search');
    };
    isNotSearchableTable = function(_idx, el) {
      return !isSearchableTable(_idx, el);
    };
    jQuery(tables).filter(isSearchableTable).each(function() {
      var $table, opts;
      $table = jQuery(this);
      opts = _.extend({}, {
        aaSorting: [],
        paging: false,
        info: false
      }, config[$table.attr('id')]);
      return jQuery($table).dataTable(opts);
    });
    return jQuery(tables).filter(isNotSearchableTable).each(function() {
      var $table, opts;
      $table = jQuery(this);
      opts = _.extend({}, {
        aaSorting: [],
        paging: false,
        info: false,
        searching: false
      }, config[$table.attr('id')]);
      return jQuery($table).dataTable(opts);
    });
  });

}).call(this);
