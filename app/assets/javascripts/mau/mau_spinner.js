// Generated by CoffeeScript 1.12.7
(function() {
  var MAU, MAUSpinner;

  MAU = window.MAU = window.MAU || {};

  MAU.SpinnerOptions = {
    color: "#5a636a",
    lines: 13,
    length: 30,
    width: 14,
    radius: 40,
    corners: 1,
    rotate: 0,
    direction: 1,
    speed: 1,
    trail: 60,
    shadow: false,
    hwaccel: false,
    className: 'spinner',
    zIndex: 2e9,
    top: '250px'
  };

  MAU.Spinner = MAUSpinner = (function() {
    function MAUSpinner(opts) {
      this.opts = opts || {};
      this.el = $(this.opts.element || "#spinner")[0];
      this.spinOpts = _.extend({}, MAU.SpinnerOptions, this.opts);
    }

    MAUSpinner.prototype.spin = function() {
      if (!this.spinner) {
        this.spinner = new Spinner(this.spinOpts);
      }
      return this.spinner.spin(this.el);
    };

    MAUSpinner.prototype.stop = function() {
      if (this.spinner) {
        return this.spinner.stop();
      }
    };

    return MAUSpinner;

  })();

}).call(this);
