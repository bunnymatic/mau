"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var flatMap_1 = require("./flatMap");
var t = require("babel-types");
/**
 * Gets the names to be bound in the LHS of an assignment.
 *
 *   a = 1;                         // [ 'a' ]
 *   ({ b, c } = {});               // [ 'b', 'c' ]
 *   [ d, e ] = [];                 // [ 'd', 'e' ]
 *   ({ f: g, h: [ i, j ] } = {});  // [ 'g', 'i', 'j' ]
 *   [ k.l, ...m ] = [];            // [ 'm' ]
 */
function getBindingIdentifiersFromLHS(node) {
    if (t.isIdentifier(node)) {
        return [node];
    }
    if (t.isObjectPattern(node)) {
        return flatMap_1.default(node.properties, function (property) {
            return getBindingIdentifiersFromLHS(t.isRestProperty(property)
                ? property.argument
                : property.value);
        });
    }
    if (t.isArrayPattern(node)) {
        return flatMap_1.default(node.elements, getBindingIdentifiersFromLHS);
    }
    if (t.isRestElement(node)) {
        return getBindingIdentifiersFromLHS(node.argument);
    }
    return [];
}
exports.default = getBindingIdentifiersFromLHS;
//# sourceMappingURL=getBindingIdentifiersFromLHS.js.map