import MagicString from 'magic-string';
import * as t from 'babel-types';
export declare type SourceMap = {
    toString(): string;
    toUrl(): string;
};
export default function addVariableDeclarations(source: string, editor?: MagicString, ast?: t.File): {
    code: string;
    map: SourceMap;
};
