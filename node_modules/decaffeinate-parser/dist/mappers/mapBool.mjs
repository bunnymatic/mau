import { Bool } from '../nodes';
import getLocation from '../util/getLocation';
export default function mapBool(context, node) {
    var _a = getLocation(context, node), line = _a.line, column = _a.column, start = _a.start, end = _a.end, raw = _a.raw;
    return new Bool(line, column, start, end, raw, JSON.parse(node.val));
}
