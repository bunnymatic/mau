import { Spread } from '../nodes';
import getLocation from '../util/getLocation';
import mapAny from './mapAny';
export default function mapSplat(context, node) {
    var _a = getLocation(context, node), line = _a.line, column = _a.column, start = _a.start, end = _a.end, raw = _a.raw;
    return new Spread(line, column, start, end, raw, mapAny(context, node.name));
}
