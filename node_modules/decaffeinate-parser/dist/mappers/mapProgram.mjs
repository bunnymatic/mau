import { Program } from '../nodes';
import mapPossiblyEmptyBlock from './mapPossiblyEmptyBlock';
export default function mapProgram(context) {
    return new Program(1, 1, 0, context.source.length, context.source, mapPossiblyEmptyBlock(context, context.ast), context);
}
