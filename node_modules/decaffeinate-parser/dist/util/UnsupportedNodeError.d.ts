import { Base } from 'decaffeinate-coffeescript/lib/coffee-script/nodes';
export default class UnsupportedNodeError extends Error {
    readonly node: Base;
    constructor(node: Base, message?: string | null);
}
