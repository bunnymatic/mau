"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var decaffeinate_parser_1 = require("decaffeinate-parser");
/**
 * Determine if there are any soak operations within this subtree of the AST.
 */
function nodeContainsSoakOperation(searchNode) {
    var foundSoak = false;
    decaffeinate_parser_1.traverse(searchNode, function (node) {
        if (foundSoak) {
            return false;
        }
        if (node.type === 'SoakedDynamicMemberAccessOp' ||
            node.type === 'SoakedFunctionApplication' ||
            node.type === 'SoakedMemberAccessOp') {
            foundSoak = true;
        }
        return true;
    });
    return foundSoak;
}
exports.default = nodeContainsSoakOperation;
