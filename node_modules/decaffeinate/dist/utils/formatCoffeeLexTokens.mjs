import { SourceType } from 'coffee-lex';
import formatRange from './formatRange';
export default function formatCoffeeLexTokens(context) {
    var tokens = context.sourceTokens;
    var resultLines = tokens.map(function (token) {
        return formatRange(token.start, token.end, context) + ": " + SourceType[token.type];
    });
    return resultLines.map(function (line) { return line + '\n'; }).join('');
}
